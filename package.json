{
  "name": "commander-builder-web-smokes",
  "version": "0.0.0",
  "private": true,
  "description": "Playwright smoke tests for the commander-builder web UI (src/commander_builder/web/static/app.js). JS test tooling only — nothing here is published or shipped with the Python package.",
  "scripts": {
    "test:e2e": "playwright test",
    "test:e2e:headed": "playwright test --headed",
    "test:e2e:report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "1.56.1"
  }
}
